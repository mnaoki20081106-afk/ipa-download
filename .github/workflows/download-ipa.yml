name: Download iOS IPA

on:
  workflow_dispatch:
    inputs:
      app_url:
        description: 'App StoreのURL (例: https://apps.apple.com/jp/app/.../id123456789)'
        required: true
        type: string

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: URLからBundle IDを抽出
        id: extract
        run: |
          APP_ID=$(echo "${{ inputs.app_url }}" | grep -o 'id[0-9]*' | cut -c 3-)
          if [ -z "$APP_ID" ]; then
            echo "エラー: 有効なApp Store URLではありません"
            exit 1
          fi
          
          BUNDLE_ID=$(curl -s "https://itunes.apple.com/lookup?id=$APP_ID" | jq -r '.results[0].bundleId')
          
          if [ "$BUNDLE_ID" == "null" ] || [ -z "$BUNDLE_ID" ]; then
            echo "エラー: Bundle IDの取得に失敗しました"
            exit 1
          fi
          
          echo "bundle_id=$BUNDLE_ID" >> $GITHUB_ENV
          echo "App ID: $APP_ID"
          echo "Bundle ID: $BUNDLE_ID"

      - name: ipatoolのインストール
        run: |
          # GitHub APIから最新リリースの情報を取得し、linux-amd64.tar.gz のURLを抽出する
          ASSET_URL=$(curl -s https://api.github.com/repos/majd/ipatool/releases/latest | jq -r '.assets[] | select(.name | endswith("linux-amd64.tar.gz")) | .browser_download_url')
          
          if [ -z "$ASSET_URL" ]; then
            echo "エラー: ダウンロードURLが取得できませんでした"
            exit 1
          fi
          
          echo "ダウンロードURL: $ASSET_URL"
          
          # 抽出したURLからダウンロードして解凍
          curl -L "$ASSET_URL" | tar xz
          
          # パスを通す
          sudo mv bin/ipatool /usr/local/bin/

      - name: 認証情報の復元 (2FA回避)
        env:
          IPATOOL_CONFIG: ${{ secrets.IPATOOL_CONFIG }}
        run: |
          mkdir -p ~/.config/ipatool
          echo "$IPATOOL_CONFIG" > ~/.config/ipatool/config.json

      - name: IPAのダウンロード
        run: |
          ipatool download -b ${{ env.bundle_id }} --purchase -o app.ipa

      - name: IPAファイルをArtifactに保存
        uses: actions/upload-artifact@v4
        with:
          name: Downloaded-IPA
          path: app.ipa
          retention-days: 1
